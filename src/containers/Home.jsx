import React from "react";
import styled from "styled-components";

function Home() {
  return <WrapperTitle>Home</WrapperTitle>;
}

const WrapperTitle = styled.h2`
  text-align: center;
  text-transform: uppercase;
`;

export default Home;
